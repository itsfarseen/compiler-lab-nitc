decl
   int a,b,c,d;
enddecl

int main()
{
  begin
  read(a);
  read(b);
  read(c);
  read(d);

  if (a<b && a<c && a<d) then
    write(a);
    write("is smallest");
  else
    write(a);
    write("is not smallest");
  endif;

  return 0;
  end
}

